PORT=8080
# Comma-separated list of allowed origins if serving frontend elsewhere
ALLOWED_ORIGINS=

# Admin dashboard token (used by admin.html)
ADMIN_TOKEN=ChooseAStrongSecret

# Optional: email notifications for new orders/messages
ADMIN_EMAIL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Optional: AI chat replies via OpenAI
OPENAI_API_KEY=
AI_MODEL=gpt-4o-mini

