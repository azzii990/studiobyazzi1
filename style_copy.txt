/* Base */
:root {
  --bg: #0b0d10;
  --bg-2: #0f1217;
  --card: #141922;
  --text: #e8eef5;
  --muted: #b3c0d4;
  --brand: #4f7cff;
  --brand-600: #3b64e6;
  --line: #1e2733;
  --ok: #29c36a;
}

* { box-sizing: border-box; }
html, body { height: 100%; }
body {
  margin: 0;
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  color: var(--text);
  background: var(--bg);
  line-height: 1.5;
}

.container { width: min(1100px, 92%); margin: 0 auto; }

/* Header */
.site-header {
  position: sticky; top: 0; z-index: 20;
  background: rgba(11,13,16,0.8); backdrop-filter: saturate(180%) blur(8px);
  border-bottom: 1px solid var(--line);
}
.header-inner { display: flex; align-items: center; gap: 16px; padding: 14px 0; }
.brand { color: var(--text); text-decoration: none; font-weight: 700; letter-spacing: 0.2px; }
.main-nav { margin-left: auto; display: none; gap: 18px; }
.main-nav a { color: var(--muted); text-decoration: none; font-weight: 500; }
.main-nav a:hover { color: var(--text); }
.header-cta { display: flex; gap: 10px; }

@media (min-width: 820px) {
  .main-nav { display: flex; }
}

/* Mobile nav */
.nav-toggle { display: none; background: transparent; color: var(--text); border: 1px solid var(--line); border-radius: 8px; padding: 8px 10px; font-size: 18px; cursor: pointer; }
@media (max-width: 819px) {
  .nav-toggle { display: inline-flex; }
  .main-nav { position: absolute; left: 0; right: 0; top: 60px; background: rgba(11,13,16,0.98); border-bottom: 1px solid var(--line); padding: 10px 16px; display: none; flex-direction: column; gap: 10px; }
  body.nav-open .main-nav { display: flex; }
}

/* Buttons */
.btn { display: inline-flex; align-items: center; justify-content: center; padding: 10px 14px; border-radius: 8px; text-decoration: none; font-weight: 600; transition: .2s; border: 1px solid transparent; cursor: pointer; }
.btn-primary { background: var(--brand); color: white; }
.btn-primary:hover { background: var(--brand-600); }
.btn-outline { background: transparent; color: var(--text); border-color: var(--line); }
.btn-outline:hover { border-color: var(--text); }
.btn-ghost { color: var(--text); background: transparent; border-color: transparent; }
.btn-ghost:hover { background: #11161f; }

/* Hero */
.hero { position: relative; min-height: 68vh; display: grid; place-items: center; background: url('images/hero-image.jpg') center/cover no-repeat; }
.hero::after { content: ""; position: absolute; inset: 0; background: linear-gradient(180deg, rgba(11,13,16,0.6), rgba(11,13,16,0.9)); pointer-events: none; }
.hero-inner { position: relative; z-index: 1; padding: 80px 0; }
.hero-text { max-width: 760px; }
.hero h1 { margin: 0 0 12px; font-size: clamp(28px, 5vw, 44px); line-height: 1.15; }
.hero p { margin: 0 0 20px; color: var(--muted); font-size: clamp(16px, 2.2vw, 18px); }
.hero-actions { display: flex; gap: 12px; flex-wrap: wrap; }

/* Sections */
.section { padding: 64px 0; background: var(--bg); }
.section-alt { padding: 64px 0; background: var(--bg-2); }
.section-title { font-size: clamp(22px, 3.2vw, 32px); margin: 0 0 10px; }
.section-lead { color: var(--muted); margin: 8px 0 0; max-width: 780px; }

/* Reveal on scroll */
.reveal { opacity: 0; transform: translateY(14px); transition: opacity .5s ease, transform .5s ease; }
.reveal.is-visible { opacity: 1; transform: translateY(0); }

/* Tabs */
.tabs { display: inline-flex; gap: 8px; padding: 6px; background: #0f141d; border: 1px solid var(--line); border-radius: 10px; margin: 18px 0 28px; }
.tab { appearance: none; background: transparent; color: var(--muted); border: 0; padding: 10px 14px; border-radius: 8px; cursor: pointer; font-weight: 600; }
.tab.active { background: #121926; color: var(--text); border: 1px solid var(--line); }
.tab-panel { display: none; }
.tab-panel.active { display: block; }

/* Cards */
.cards { display: grid; grid-template-columns: 1fr; gap: 18px; }
@media (min-width: 820px) { .cards { grid-template-columns: 1fr 1fr; } }
.card { background: var(--card); border: 1px solid var(--line); border-radius: 14px; padding: 20px; }
.card h3 { margin: 0 0 6px; }
.card p { margin: 0 0 12px; color: var(--muted); }
.features { list-style: none; padding: 0; margin: 0 0 14px; }
.features li { padding-left: 18px; position: relative; margin: 8px 0; color: var(--text); }
.features li::before { content: ""; position: absolute; left: 0; top: 10px; width: 8px; height: 8px; background: var(--ok); border-radius: 50%; }
.card-footer { display: flex; align-items: center; justify-content: space-between; gap: 10px; margin-top: 6px; }
.price { color: var(--text); font-weight: 700; }

/* Portfolio */
.grid { display: grid; gap: 16px; }
.portfolio-grid { grid-template-columns: repeat(2, 1fr); }
@media (min-width: 900px) { .portfolio-grid { grid-template-columns: repeat(4, 1fr); } }
.portfolio-item { overflow: hidden; border-radius: 12px; border: 1px solid var(--line); background: #0f141d; }
.portfolio-item img { width: 100%; display: block; aspect-ratio: 4/3; object-fit: cover; transition: transform .3s ease; }
.portfolio-item:hover img { transform: scale(1.04); }
.portfolio-item figcaption { padding: 10px 12px; color: var(--muted); font-size: 14px; border-top: 1px solid var(--line); }

/* Pricing */
.pricing { display: grid; grid-template-columns: 1fr; gap: 18px; }
@media (min-width: 820px) { .pricing { grid-template-columns: 1fr 1fr; } }
.pricing-column { background: var(--card); border: 1px solid var(--line); border-radius: 14px; padding: 18px; }
.pricing-column h3 { margin: 0 0 12px; }
.pricing-column ul { list-style: none; padding: 0; margin: 0; }
.pricing-column li { display: flex; align-items: center; justify-content: space-between; padding: 10px 0; border-bottom: 1px dashed #243041; color: var(--text); }
.pricing-column li:last-child { border-bottom: 0; }
.pricing .note, .note { color: var(--muted); margin-top: 12px; }

/* Payment */
.payment-box { background: var(--card); border: 1px solid var(--line); border-radius: 14px; padding: 18px; }
.payment-methods { display: grid; grid-template-columns: 1fr; gap: 14px; }
@media (min-width: 820px) { .payment-methods { grid-template-columns: repeat(3, 1fr); } }
.method { background: #101621; border: 1px solid var(--line); border-radius: 12px; padding: 16px; }
.method h3 { margin: 0 0 6px; }
.method p { margin: 0 0 12px; color: var(--muted); }
code { background: #0b111a; border: 1px solid var(--line); padding: 2px 6px; border-radius: 6px; color: var(--text); }

/* Quick Pay */
.quick-pay { border: 1px solid var(--line); border-radius: 14px; padding: 18px; background: var(--card); }
.qp-grid { display: grid; grid-template-columns: 1fr; gap: 12px; }
@media (min-width: 640px) { .qp-grid { grid-template-columns: 1fr 1fr; } }
.qp-grid label { display: grid; gap: 6px; color: var(--muted); font-weight: 600; }
.qp-grid input { background: #0f141d; border: 1px solid var(--line); color: var(--text); border-radius: 8px; padding: 10px; font: inherit; }
.qp-grid input:focus { outline: 2px solid var(--brand); border-color: var(--brand); }
.qp-actions { display: flex; gap: 10px; align-items: center; }

/* Contact */
.contact { display: grid; grid-template-columns: 1fr; gap: 18px; align-items: start; }
@media (min-width: 900px) { .contact { grid-template-columns: 1.1fr 1fr; } }
.contact-list { list-style: none; padding: 0; margin: 12px 0 0; }
.contact-list li { margin: 6px 0; }
.contact-list a { color: var(--text); text-decoration: none; }
.contact-form { background: var(--card); border: 1px solid var(--line); border-radius: 12px; padding: 16px; display: grid; gap: 12px; }
.contact-form label { display: grid; gap: 6px; font-weight: 600; color: var(--muted); }
.contact-form input, .contact-form select, .contact-form textarea {
  background: #0f141d; border: 1px solid var(--line); color: var(--text);
  border-radius: 8px; padding: 10px; font: inherit;
}
.contact-form input:focus, .contact-form select:focus, .contact-form textarea:focus { outline: 2px solid var(--brand); border-color: var(--brand); }

/* Footer */
.site-footer { border-top: 1px solid var(--line); background: #0b0f16; }
.footer-inner { display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 18px 0; }
.socials { display: flex; gap: 14px; }
.socials a { color: var(--muted); text-decoration: none; }
.socials a:hover { color: var(--text); }

/* Modal */
.modal { position: fixed; inset: 0; display: none; }
.modal.open { display: block; }
.modal__backdrop { position: absolute; inset: 0; background: rgba(0,0,0,.6); }
.modal__dialog { position: relative; margin: 6vh auto; width: min(640px, 92%); background: var(--card); border: 1px solid var(--line); border-radius: 12px; padding: 18px; z-index: 2; }
.modal__close { position: absolute; right: 10px; top: 8px; font-size: 24px; line-height: 1; background: transparent; color: var(--muted); border: 0; cursor: pointer; }
.order-form { display: grid; gap: 12px; margin-top: 10px; }
.order-form label { display: grid; gap: 6px; color: var(--muted); font-weight: 600; }
.order-form input, .order-form textarea { background: #0f141d; border: 1px solid var(--line); color: var(--text); border-radius: 8px; padding: 10px; font: inherit; }
.order-form input:focus, .order-form textarea:focus { outline: 2px solid var(--brand); border-color: var(--brand); }

/* Tables (Admin) */
.table { border-collapse: collapse; width: 100%; }
.table th, .table td { border-bottom: 1px solid var(--line); padding: 10px; text-align: left; }
.table thead th { color: var(--muted); font-weight: 700; background: #0f141d; position: sticky; top: 0; }
.status-select { background: #0f141d; border: 1px solid var(--line); color: var(--text); border-radius: 8px; padding: 6px 8px; }

/* Chat widget */
.chat-launcher { position: fixed; right: 16px; bottom: 16px; z-index: 40; border-radius: 999px; width: 56px; height: 56px; display: flex; align-items: center; justify-content: center; background: var(--brand); color: #fff; border: 1px solid var(--line); box-shadow: 0 6px 24px rgba(0,0,0,.3); cursor: pointer; }
.chat-launcher:hover { background: var(--brand-600); }
.chat-window { position: fixed; right: 16px; bottom: 84px; width: min(360px, 92vw); background: var(--card); border: 1px solid var(--line); border-radius: 12px; z-index: 40; box-shadow: 0 10px 30px rgba(0,0,0,.35); display: none; overflow: hidden; }
.chat-window.open { display: grid; grid-template-rows: auto 1fr auto; }
.chat-header { padding: 10px 12px; background: #0f141d; border-bottom: 1px solid var(--line); display: flex; align-items: center; justify-content: space-between; }
.chat-header h3 { margin: 0; font-size: 16px; }
.chat-body { padding: 10px; max-height: 50vh; overflow: auto; display: grid; gap: 8px; }
.chat-msg { display: inline-flex; max-width: 85%; padding: 8px 10px; border-radius: 12px; line-height: 1.3; }
.chat-msg.user { margin-left: auto; background: #1b2440; }
.chat-msg.bot { margin-right: auto; background: #0f141d; border: 1px solid var(--line); }
.chat-form { display: grid; grid-template-columns: 1fr auto; gap: 8px; padding: 10px; border-top: 1px solid var(--line); }
.chat-form input, .chat-form textarea { background: #0f141d; border: 1px solid var(--line); color: var(--text); border-radius: 8px; padding: 8px; font: inherit; }
.chat-form button { white-space: nowrap; }

/* Lightbox */
.lightbox { position: fixed; inset: 0; display: none; align-items: center; justify-content: center; background: rgba(0,0,0,.8); z-index: 50; }
.lightbox.open { display: flex; }
.lightbox img { max-width: 92vw; max-height: 90vh; border-radius: 10px; border: 1px solid var(--line); }

